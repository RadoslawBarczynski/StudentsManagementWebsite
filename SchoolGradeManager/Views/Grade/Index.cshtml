@model IEnumerable<SchoolGradeManager.Models.Grade>

@{
    ViewData["Title"] = "Index";
}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>



<h1 class="h2" style="text-align: center">Tabela ocen</h1>

<table class="table table-striped table-dark">
    <thead>
        <tr style="text-align: center">
            <th>
                Student
            </th>
            <th>
                Wartość punktowa
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr style="text-align: center">
                <td>
                    @Html.DisplayFor(model => item.student.StudentFirstName) @Html.DisplayFor(model => item.student.StudentLastName)
                </td>
                <td>
                    @Html.DisplayFor(model => item.G_Score)
                </td>
                <td>
                    @Html.ActionLink("Eytuj", "Edit", new {  id=item.GradeId  }, new {@class = "btn btn-primary"})
                    @Html.ActionLink("Usuń", "Delete", new {  id=item.GradeId  }, new {@class = "btn btn-danger"})
                </td>
            </tr>
        }
    </tbody>
</table>


